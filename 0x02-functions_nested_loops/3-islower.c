#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 *_islower -check for loweralphabets.
 *
 * Return: Always 0.
 */

int _islower(int c)
{
		if ((c >= 97) && (c <= 122))

		{
		return (1);

		}

		else

		{
		return (0);

		}
	
	return (0);
}
