#include <stdio.h>
#include <stdlib.h>
/**
 *main -enter point to check the code
 *@i:integer
 *
 *Always return 0.
 */
int main(void)
{
	int i;

	for(i = 0; i < 12; i++)
	{
		pass[i] = 33 + rand() % 94;
	}
	pass[i] = '\0';
	printf("%s\n",pass);
	return (0);
}
	
